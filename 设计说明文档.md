# YouTube 视频下载与微信视频号自动发布系统
## 设计说明文档（技术视角）

---

## 一、系统架构

### 1.1 整体架构

系统采用**消息驱动 + 多阶段管线**架构：

- **消息层**：通过飞书 IM API 接收用户指令，发送执行通知
- **调度层**：`task_engine.py` 负责任务登记、调度、执行
- **执行层**：各阶段通过子进程调用独立脚本（下载器、自动化引擎等）
- **适配层**：通过适配器实现阶段间的数据转换与传递

### 1.2 核心设计原则

1. **配置驱动**：任务类型、表单字段、阶段命令均通过 JSON 配置，无需修改代码
2. **阶段独立**：每个阶段功能独立完整，可单独测试和执行
3. **适配器解耦**：阶段间数据传递通过适配器完成，引擎统一管理
4. **并发安全**：通过 stdin 传递配置，避免多任务文件冲突
5. **断点续传**：下载失败后可重新运行继续下载

---

## 二、核心模块设计

### 2.1 任务引擎（task_engine.py）

#### 职责
- 接收飞书消息，解析用户意图（登记/查询/取消/删除/重试）
- 管理任务生命周期（pending → running → success/failed）
- 调度到期任务，执行多阶段管线

#### 关键函数

| 函数 | 说明 |
|------|------|
| `load_task_configs()` | 加载任务类型定义 |
| `create_task()` | 创建任务，构建执行计划 `exec_plan` |
| `execute_multi_stage()` | 多阶段执行器 |
| `run_subprocess()` | 子进程执行，支持 stdin 传入配置 |
| `load_adapter()` | 动态加载适配器 |
| `handle_message()` | 消息路由与意图识别 |
| `scheduler_loop()` | 定时调度器（后台线程） |

#### 任务状态机

```
pending ──→ running ──→ success
              │             ↑
              └──→ failed ──┘ (重试)
```

#### 阶段状态机

```
pending → running → success
                └──→ failed
```

### 2.2 飞书通信模块（feishu_send.py）

#### 职责
- 封装飞书 CLI 命令调用
- 提供消息发送接口
- 管理聊天列表配置

#### 关键函数

| 函数 | 说明 |
|------|------|
| `send_text()` | 发送文本 |
| `send_markdown()` | 发送 Markdown（转飞书 post 格式） |
| `list_messages()` | 获取消息列表 |
| `extract_display_text()` | 提取消息纯文本 |
| `markdown_to_post()` | Markdown → 飞书富文本 |

### 2.3 YouTube 下载器（yt_download.py）

#### 职责
- 调用 `yt-dlp` 下载视频
- 支持断点续传、代理、cookies
- 后处理：webm 转 mp4、提取音频、保存信息文件

#### 配置（`yt_config.json`）

| 配置项 | 说明 |
|--------|------|
| `代理` | SOCKS5/HTTP 代理设置 |
| `认证.cookies文件` | YouTube cookies 路径 |
| `下载.格式选择` | 视频格式选择表达式 |
| `断点续传` | 启用/重试次数/重试间隔 |
| `信息文件` | 是否保存、包含字段 |

#### 退出码

- `0`：成功
- `1`：存在失败（供引擎判断）

### 2.4 桌面自动化引擎（run_workflow.py）

#### 职责
- 读取工作流配置，执行自动化操作
- 模板匹配定位屏幕元素
- 模拟鼠标键盘操作

#### 工作流程

1. 查找目标进程窗口
2. 前置并最大化
3. 截屏
4. 遍历步骤：匹配点位 → 执行动作 → 截屏记录
5. 返回成功/失败

#### 配置输入方式

- `--config wf.json`：从文件读取
- `--stdin`：从标准输入读取（推荐）

### 2.5 模板匹配引擎（template_matcher.py）

#### 职责
- OpenCV 模板匹配
- 非极大值抑制去重
- 支持中文路径

#### 匹配结果

```python
{
    "左上角": (x, y),
    "尺寸": (w, h),
    "中心点": (cx, cy),
    "置信度": 0.95
}
```

---

## 三、适配器设计

### 3.1 适配器接口

每个适配器导出两个函数：

```python
def prepare_input(context):
    """阶段执行前调用，准备输入数据"""
    return context

def process_output(stdout, context):
    """阶段执行后调用，处理结果"""
    return context
```

### 3.2 publish_prepare 适配器

**绑定**：任务类型2的阶段2

**功能流程**：

```
prepare_input(context):
    1. 去重检查（.publish_history.json）
    2. 查找视频文件（downloads/ 最新 .mp4）
    3. 解析元数据（_info.txt）
    4. 标题清理（_sanitize_title）
       - 保留：字母数字、中文、书名号、引号、冒号、加号、问号、百分号、摄氏度
       - 逗号 → 空格
       - 其他符号 → 删除
    5. 标题长度处理
       - 最短 6 字：循环填充（"AB" → "ABABABAB"）
       - 最长 16 字：截断
    6. 描述组装（_build_description）
       - 标签行前置：`#标签1 #标签2`
       - 换行 + 描述内容
       - 总长不超过 900 字符
    7. 复制视频到发布目录（WeChatAppEx/）
    8. 渲染模板（wf_publish.template.json）
    9. 写入 context["stdin_data"]
```

---

## 四、数据流转设计

### 4.1 任务登记流程

```
用户: "登记任务"
    → handle_message() 匹配意图 "登记"
    → 列出任务类型
    → 用户选择编号或名称
    → 状态: waiting_task_type → waiting_form
    → 用户回复表单（字段名: 值）
    → parse_form_reply() 解析
    → create_task() 创建任务
    → 写入 tasks.json
    → 通知用户
```

### 4.2 多阶段执行流程（任务类型2）

```
scheduler_loop() 检测任务到期
    → task_queue.put()
    → execute_task()
    → execute_multi_stage()

    阶段1: 下载视频
    cmd: ["python", "yt_download.py", URL]
    → run_subprocess()
    → 检查 returncode + ERROR: 行
    → 更新 stage_results

    [输入准备适配器: publish_prepare]
    → 查找视频、解析元数据
    → 清理标题、组装描述
    → 复制视频、渲染模板
    → context["stdin_data"] = 配置JSON

    阶段2: 发布到视频号
    cmd: ["python", "run_workflow.py", "--stdin"]
    stdin = context["stdin_data"]
    → run_subprocess(cmd, stdin_data=stdin)
    → template_matcher 定位点位
    → pyautogui 模拟操作
    → 返回成功/失败

    [后处理适配器: publish_prepare]
    → 标记已发布
    → 清理 context 临时数据

    → 更新任务状态
    → 通知用户
```

### 4.3 context 数据结构

```python
context = {
    "task_id": "T-20260627-001",
    "fields": {                    # 用户表单字段
        "video_url": "https://...",
        "title": "视频标题",
        "description": "描述",
        "tags": "AI,教程"
    },
    "type_config": {               # 任务类型配置
        "发布模板路径": "wf_publish.template.json",
        "标题截断长度": 30
    },
    # 以下是适配器动态写入
    "publish_config": "...",       # 渲染后的配置JSON
    "publish_video_path": "...",   # 发布视频路径
    "publish_title": "...",        # 清理后的标题
    "publish_description": "...",  # 组装后的描述
    "stdin_data": "...",           # 传给 run_workflow 的配置
    "skip_publish": False          # 是否跳过发布
}
```

---

## 五、配置设计

### 5.1 任务类型配置（task_configs.json）

#### 单阶段任务

```json
{
  "配置映射": {
    "命令行": ["python", "yt_download.py", "{fields.video_url}"]
  }
}
```

#### 多阶段任务

```json
{
  "类型配置": {
    "发布模板路径": "wf_publish.template.json",
    "标题截断长度": 30
  },
  "配置映射": {
    "阶段列表": [
      {
        "阶段名": "下载视频",
        "命令行": ["python", "yt_download.py", "{fields.video_url}"],
        "失败策略": "中止",
        "超时秒": 1800,
        "后处理适配器": "yt_download_to_publish"
      },
      {
        "阶段名": "发布到视频号",
        "命令行": ["python", "run_workflow.py", "--stdin"],
        "输入准备适配器": "publish_prepare",
        "失败策略": "通知继续",
        "超时秒": 600
      }
    ]
  }
}
```

#### 占位符语法

| 占位符 | 替换来源 |
|--------|----------|
| `{fields.xxx}` | 表单字段值 |
| `{task_id}` | 任务ID |

### 5.2 工作流模板变量

`wf_publish.template.json` 中的变量：

| 变量 | 来源 |
|------|------|
| `{{title}}` | 表单字段 / 视频元数据 |
| `{{description}}` | 适配器组装（标签 + 描述） |
| `{{video_dir}}` | 发布目录绝对路径 |
| `{{video_file}}` | 视频文件名 |

---

## 六、容错与并发设计

### 6.1 并发安全

**问题**：多任务同时发布时，共享配置文件会冲突。

**解决**：使用 `--stdin` 模式，配置通过标准输入传递，不依赖共享文件。

### 6.2 去重设计

| 层级 | 机制 |
|------|------|
| 下载去重 | `.downloaded` 记录文件 |
| 发布去重 | `.publish_history.json` |

### 6.3 失败策略

| 策略 | 说明 |
|------|------|
| `中止` | 失败后不执行后续阶段 |
| `通知继续` | 失败后继续执行 |

### 6.4 重试设计

重试时只重置失败阶段状态，已成功阶段自动跳过。

---

## 七、扩展说明

### 新增任务类型

1. 在 `task_configs.json` 中添加新的任务类型定义
2. 如需多阶段，在 `配置映射.阶段列表` 中定义各阶段
3. 如需阶段间适配，编写适配器并放入 `adapters/` 目录
4. 无需修改 `task_engine.py`

### 新增工作流步骤

直接编辑 `wf_publish.template.json`，添加新的步骤对象，无需修改代码。
