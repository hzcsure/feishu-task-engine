# YouTube 视频下载与微信视频号自动发布系统
## 数据字典

---

## 一、说明

本文档收录系统中所有**程序内定义和使用的变量、配置项、数据结构的含义与类型**。

按以下分类组织：
1. **配置文件变量**（JSON 配置中的字段）
2. **任务数据变量**（`tasks.json` 中的字段）
3. **程序内存变量**（Python 代码中定义的关键变量）
4. **上下文变量**（阶段间传递的 context 字段）

---

## 二、配置文件变量

### 2.1 task_configs.json（任务类型定义）

#### 顶层结构

| 字段 | 类型 | 说明 |
|------|------|------|
| `任务类型` | `list[dict]` | 任务类型定义数组 |

#### 单个任务类型对象

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `名称` | `string` | 是 | 任务类型显示名称，如「下载YouTube视频」 |
| `标识` | `string` | 是 | 任务类型唯一标识，如 `yt_download` |
| `描述` | `string` | 否 | 任务类型描述 |
| `要素列表` | `list[dict]` | 是 | 登记表单字段定义 |
| `类型配置` | `dict` | 否 | 该类型下所有任务共享的配置 |
| `配置映射` | `dict` | 是 | 执行命令映射 |

#### 要素列表（表单字段）对象

| 字段 | 类型 | 说明 |
|------|------|------|
| `字段` | `string` | 字段 key，用于 `{fields.xxx}` 引用 |
| `显示名` | `string` | 表单中显示的名称 |
| `类型` | `string` | `text` / `textarea` / `datetime` |
| `必填` | `bool` | 是否必填 |
| `默认` | `string` | 默认值 |
| `提示` | `string` | 填写提示 |

#### 类型配置（类型配置）对象

| 字段 | 类型 | 说明 | 适用任务类型 |
|------|------|------|-------------|
| `发布模板路径` | `string` | 发布工作流模板文件路径 | 类型2 |
| `发布目录` | `string` | 视频发布目录 | 类型2 |
| `标题截断长度` | `int` | 标题最大长度 | 类型2 |
| `描述截断长度` | `int` | 描述最大长度 | 类型2 |

#### 配置映射对象

**单阶段任务**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `命令行` | `list[string]` | 执行命令，支持 `{fields.xxx}` 占位符 |

**多阶段任务**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `阶段列表` | `list[dict]` | 阶段定义数组 |

#### 阶段定义对象

| 字段 | 类型 | 说明 |
|------|------|------|
| `阶段名` | `string` | 阶段显示名称 |
| `命令行` | `list[string]` | 执行命令 |
| `失败策略` | `string` | `中止` / `通知继续` |
| `超时秒` | `int` | 阶段执行超时时间（秒） |
| `输入准备适配器` | `string` | 适配器模块名，阶段执行前调用 |
| `后处理适配器` | `string` | 适配器模块名，阶段执行后调用 |

---

### 2.2 yt_config.json（YouTube 下载配置）

| 字段 | 类型 | 说明 |
|------|------|------|
| `代理.启用` | `bool` | 是否启用代理 |
| `代理.协议` | `string` | `socks5` / `http` |
| `代理.地址` | `string` | 代理服务器地址 |
| `代理.端口` | `int` | 代理服务器端口 |
| `认证.cookies文件` | `string` | cookies 文件路径 |
| `工具路径.yt-dlp` | `string` | yt-dlp 可执行文件路径 |
| `工具路径.ffmpeg` | `string` | ffmpeg 路径 |
| `下载.格式选择` | `string` | yt-dlp 格式选择表达式 |
| `下载.合并格式` | `string` | 合并输出格式，如 `mp4` |
| `下载.文件名模板` | `string` | 输出文件名模板 |
| `下载.同时下载片段数` | `int` | 并发片段数 |
| `断点续传.启用` | `bool` | 是否启用断点续传 |
| `断点续传.重试次数` | `int` | 下载失败重试次数 |
| `信息文件.启用` | `bool` | 是否保存信息文件 |
| `信息文件.格式` | `string` | 信息文件格式（`txt`） |
| `信息文件.包含字段` | `list[string]` | 信息文件中包含的字段 |
| `后处理.webm转mp4` | `bool` | 是否转码 webm → mp4 |
| `后处理.保留音频` | `bool` | 是否提取音频 |

---

### 2.3 feishu_config.json（飞书配置）

| 字段 | 类型 | 说明 |
|------|------|------|
| `默认聊天名称` | `string` | 默认发送消息的聊天名称 |
| `默认身份` | `string` | `bot` / `user` |
| `聊天列表` | `list[dict]` | 聊天对象列表 |
| `聊天列表[].名称` | `string` | 聊天显示名称 |
| `聊天列表[].chat_id` | `string` | 飞书 chat_id |
| `聊天列表[].备注` | `string` | 备注说明 |

---

### 2.4 wf_publish.template.json（发布工作流模板）

| 字段 | 类型 | 说明 |
|------|------|------|
| `进程名` | `string` | 目标进程名，如 `WeChatAppEx` |
| `工作流` | `list[dict]` | 工作流步骤数组 |

#### 工作流步骤对象

| 字段 | 类型 | 说明 |
|------|------|------|
| `步骤` | `int` | 步骤编号 |
| `描述` | `string` | 步骤说明 |
| `点位图` | `string` | 模板图片文件名 |
| `动作` | `string` | `单击` / `回车`（为空则只定位不操作） |
| `文本输入` | `string` | 要输入的文本（支持 `{{变量}}`） |
| `点击后等待` | `float` | 动作后等待秒数 |
| `截屏标志` | `string` | `"1"` 表示截屏，`"0"` 表示不截 |
| `选点` | `string` | `中心点` / `左上角` / `左下角` 等 |
| `必要点位` | `list` | `[模板文件名, 最大重试次数]` |
| `失败处理` | `string` | `中止` / `跳过` |
| `滚屏` | `int` | 滚屏半屏数，正数向上，负数向下 |

---

## 三、任务数据变量（tasks.json）

### 3.1 顶层结构

| 字段 | 类型 | 说明 |
|------|------|------|
| `tasks` | `dict` | 任务字典，key 为任务ID |
| `counter` | `int` | 当日任务计数器 |
| `counter_date` | `string` | 计数器日期 `YYYY-MM-DD` |

### 3.2 单个任务对象

| 字段 | 类型 | 说明 |
|------|------|------|
| `type` | `string` | 任务类型标识 |
| `type_name` | `string` | 任务类型名称 |
| `type_config` | `dict` | 任务类型配置（从 task_configs.json 复制） |
| `status` | `string` | `pending` / `running` / `success` / `failed` / `cancelled` |
| `fields` | `dict` | 用户表单字段值 |
| `exec_plan` | `list[dict]` | 执行计划（阶段列表） |
| `stage_results` | `dict` | 各阶段执行结果 |
| `execute_at` | `string` | 计划执行时间 `YYYY-MM-DD HH:MM` |
| `created_at` | `string` | 创建时间 |
| `started_at` | `string` | 开始执行时间 |
| `finished_at` | `string` | 完成时间 |
| `result` | `string` | 执行结果描述 |
| `error` | `string` | 错误信息 |
| `chat_id` | `string` | 发起任务的聊天ID |
| `created_by` | `string` | 发起人标签 |

### 3.3 阶段结果对象（stage_results 值）

| 字段 | 类型 | 说明 |
|------|------|------|
| `status` | `string` | `pending` / `running` / `success` / `failed` |
| `started_at` | `string` | 阶段开始时间 |
| `finished_at` | `string` | 阶段完成时间 |
| `error` | `string` | 阶段错误信息 |

---

## 四、程序内存变量

### 4.1 task_engine.py

| 变量 | 类型 | 说明 |
|------|------|------|
| `SCRIPT_DIR` | `Path` | 脚本所在目录 |
| `TASK_CONFIG_FILE` | `Path` | 任务配置文件路径 |
| `TASKS_FILE` | `Path` | 任务数据文件路径 |
| `user_states` | `dict` | 用户对话状态，`{chat_id: {state, context}}` |
| `known_msg_ids` | `set` | 已知消息ID集合（去重） |
| `task_queue` | `Queue` | 待执行任务队列 |
| `exit_event` | `Event` | 退出信号（聊天模式） |

#### user_states 状态值

| 状态值 | 说明 |
|--------|------|
| `idle` | 空闲，等待新命令 |
| `waiting_task_type` | 已发送任务类型列表，等待用户选择 |
| `waiting_form` | 已发送表单，等待用户回复 |

---

### 4.2 feishu_send.py

| 变量 | 类型 | 说明 |
|------|------|------|
| `CONFIG_FILE` | `Path` | 飞书配置文件路径 |
| `LARK_CLI` | `string` | lark-cli 路径（全局缓存） |
| `NODE_EXE` | `string` | node.exe 路径（全局缓存） |

---

### 4.3 yt_download.py

| 变量 | 类型 | 说明 |
|------|------|------|
| `DEFAULT_CONFIG` | `Path` | 默认配置文件路径 `yt_config.json` |
| `SCRIPT_DIR` | `Path` | 脚本所在目录 |
| `video_id` | `string` | YouTube 视频ID（运行时） |
| `safe_title` | `string` | 安全文件名（去除特殊字符） |

---

### 4.4 run_workflow.py

| 变量 | 类型 | 说明 |
|------|------|------|
| `SCRIPT_DIR` | `string` | 脚本所在目录 |
| `DEFAULT_WF` | `string` | 默认配置文件路径 `wf.json` |
| `process_name` | `string` | 目标进程名 |
| `screenshot_path` | `string` | 截图保存路径 |
| `success` | `bool` | 工作流执行是否成功 |

---

### 4.5 template_matcher.py

| 变量 | 类型 | 说明 |
|------|------|------|
| `source` | `numpy.ndarray` | 源图（灰度） |
| `template` | `numpy.ndarray` | 模板图（灰度） |
| `result` | `numpy.ndarray` | 匹配结果矩阵 |
| `threshold` | `float` | 置信度阈值，默认 `0.8` |

#### find_text() 返回结果结构

| 字段 | 类型 | 说明 |
|------|------|------|
| `左上角` | `tuple(int,int)` | 匹配区域左上角坐标 |
| `尺寸` | `tuple(int,int)` | 匹配区域宽高 |
| `中心点` | `tuple(int,int)` | 匹配区域中心坐标 |
| `置信度` | `float` | 匹配置信度（0~1） |

---

## 五、上下文变量（context）

`context` 是阶段间传递数据的字典，贯穿 `execute_multi_stage()` 全过程。

### 5.1 初始 context（引擎写入）

| 字段 | 类型 | 说明 |
|------|------|------|
| `task_id` | `string` | 任务ID |
| `fields` | `dict` | 用户表单字段 |
| `type_config` | `dict` | 任务类型配置 |

### 5.2 适配器写入的变量

| 字段 | 类型 | 写入适配器 | 说明 |
|------|------|-----------|------|
| `publish_config` | `string` | `publish_prepare` | 渲染后的发布配置JSON字符串 |
| `publish_video_path` | `string` | `publish_prepare` | 发布视频完整路径 |
| `publish_title` | `string` | `publish_prepare` | 清理后的标题 |
| `publish_description` | `string` | `publish_prepare` | 组装后的描述 |
| `publish_video_url` | `string` | `publish_prepare` | 原始视频URL（用于去重） |
| `video_filename` | `string` | `yt_download_to_publish` | 视频文件名 |
| `video_dir_abs` | `string` | `yt_download_to_publish` | 发布目录绝对路径 |
| `stdin_data` | `string` | `publish_prepare` | 传给 run_workflow.py 的配置 |
| `skip_publish` | `bool` | `publish_prepare` | 是否跳过发布（去重时） |

---

## 六、外部数据文件

### 6.1 .publish_history.json（发布历史）

| 字段 | 类型 | 说明 |
|------|------|------|
| `published_urls` | `list[string]` | 已发布的视频URL列表 |

**位置**：项目根目录

---

### 6.2 视频信息文件（_info.txt）

由 `yt_download.py` 在下载完成后自动生成，保存在 `downloads/` 目录。

| 字段 | 说明 |
|------|------|
| `标题` | 视频标题 |
| `视频ID` | YouTube 视频ID |
| `URL` | 视频链接 |
| `时长` | 视频时长（HH:MM:SS） |
| `上传者` | 频道名称 |
| `上传日期` | 上传日期 |
| `描述` | 视频描述（前200字） |

---

## 七、变量命名约定

### 7.1 前缀约定

| 前缀 | 说明 | 示例 |
|------|------|------|
| `_` | 模块私有函数 | `_sanitize_title()` |
| `_find_` | 查找类函数 | `_find_latest_download()` |
| `_parse_` | 解析类函数 | `_parse_info_file()` |
| `_build_` | 组装类函数 | `_build_description()` |
| `_render_` | 模板渲染函数 | `_render_template_string()` |
| `_load_` | 加载函数 | `_load_publish_history()` |
| `_save_` | 保存函数 | `_save_publish_history()` |
| `_is_` | 判断函数 | `_is_already_published()` |
| `_mark_` | 标记函数 | `_mark_as_published()` |

### 7.2 缩写约定

| 缩写 | 全称 | 说明 |
|------|------|------|
| `ctx` | context | 阶段间上下文 |
| `cfg` | config | 配置 |
| `proc` | process | 进程 |
| `elem` | element | 元素 |
| `msg` | message | 消息 |
| `stmt` | statement | SQL语句（如有） |
| `util` | utility | 工具函数 |
| `tmp` | temporary | 临时变量 |
| `opt` | option | 选项 |
| `req` | request | 请求 |
| `resp` | response | 响应 |
| `err` | error | 错误 |
| `ok` | success | 是否成功 |
| `val` | value | 值 |
| `idx` | index | 索引 |
| `elem` | element | 元素 |
| `info` | information | 信息 |
| `meta` | metadata | 元数据 |
| `stdout_lines` | - | 子进程标准输出行列表 |
| `stdin_data` | - | 传给子进程的标准输入数据 |
| `pub` | publish | 发布 |
| `dl` | download | 下载 |
| `wf` | workflow | 工作流 |
| `tpl` | template | 模板 |

---

## 八、数据类型汇总

### 8.1 基本类型

| 类型 | 说明 | 示例 |
|------|------|------|
| `string` | 字符串 | `"T-20260627-001"` |
| `int` | 整数 | `30` |
| `float` | 浮点数 | `0.8` |
| `bool` | 布尔 | `true` / `false` |
| `list` | 数组 | `[1, 2, 3]` |
| `dict` | 对象 | `{"key": "value"}` |
| `Path` | 路径对象 | `Path("downloads/video.mp4")` |

### 8.2 枚举值

#### 任务状态（status）

| 值 | 说明 |
|------|------|
| `pending` | 待执行 |
| `running` | 执行中 |
| `success` | 成功 |
| `failed` | 失败 |
| `cancelled` | 已取消 |

#### 阶段失败策略

| 值 | 说明 |
|------|------|
| `中止` | 失败后终止后续阶段 |
| `通知继续` | 失败后继续执行 |

#### 表单字段类型

| 值 | 说明 |
|------|------|
| `text` | 单行文本 |
| `textarea` | 多行文本 |
| `datetime` | 日期时间 |

---

## 九、版本与维护记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-06-22 | v1.0 | 初始版本，支持单阶段下载 |
| 2026-06-27 | v2.0 | 增加多阶段管线、适配器架构 |
| 2026-06-30 | v2.1 | 整理文档，清理无关文件 |

---

*本文档随代码变更同步更新，请确保与代码版本一致。*
