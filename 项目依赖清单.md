
# 项目依赖清单

## 系统环境

- **操作系统**: Windows（必须，Win32 API）
- **Python**: 3.12+（必选，所有脚本运行基础）
- **Node.js**: 可选（lark-cli 需要，飞书通信依赖）

---

## Python 标准库（内置，无需安装）

| 模块 | 用途 | 被谁引用 |
|------|------|---------|
| os | 路径、文件操作 | 全部脚本 |
| sys | 运行时参数、路径 | 全部脚本 |
| json | JSON 读写、序列化 | 全部脚本 |
| re | 正则匹配 | task_engine, feishu_send, yt_download, publish_prepare, yt_download_to_publish |
| time | 延时、时间戳 | task_engine, feishu_send, yt_download, update_cookies, run_workflow |
| subprocess | 执行子进程 | task_engine, feishu_send, yt_download |
| argparse | 命令行参数 | task_engine, feishu_send, yt_download, update_cookies, run_workflow |
| threading | 多线程 | task_engine, feishu_send |
| copy | 深拷贝 | task_engine |
| shutil | 文件复制 | publish_prepare |
| ctypes | Win32 API 调用 | run_workflow |
| io | 文本流编码 | run_workflow |
| pathlib.Path | 路径对象化 | 全部脚本 |
| datetime | 日期时间 | task_engine, yt_download, update_cookies |
| queue.Queue | 消息队列 | task_engine, feishu_send |

---

## 第三方 Python 包（需 pip install）

### 核心依赖

| 包名 | pip 命令 | 版本参考 | 用途 | 被谁引用 |
|------|---------|---------|------|---------|
| opencv-python | `pip install opencv-python` | >=4.5 | 图像模板匹配（模版匹配算法） | template_matcher |
| numpy | `pip install numpy` | >=1.20 | 图像矩阵运算 | template_matcher |
| pyautogui | `pip install pyautogui` | >=0.9 | 桌面自动化：鼠标点击、键盘输入 | run_workflow |
| pyperclip | `pip install pyperclip` | >=1.8 | 剪贴板操作：粘贴中文文本 | run_workflow |
| Pillow | `pip install Pillow` | >=9.0 | 图像截取、处理（ImageGrab） | run_workflow |
| psutil | `pip install psutil` | >=5.8 | 进程信息查询 | run_workflow |
| pywin32 | `pip install pywin32` | >=300 | Windows 窗口管理（win32gui, win32con 等） | run_workflow |

### 功能模块依赖

| 包名 | pip 命令 | 用途 | 被谁引用 |
|------|---------|------|---------|
| playwright | `pip install playwright` | 浏览器自动化（更新 YouTube cookies） | update_cookies |

---

## 外部系统工具

### 必选

| 工具 | 用途 | 被谁调用 | 备注 |
|------|------|---------|------|
| **lark-cli** | 飞书 CLI 通信工具，收发消息 | feishu_send.py | 由 WorkBuddy 统一管理，位于 `~/.workbuddy/binaries/node/cli-connector-packages/`；运行时需飞书账号已认证 |
| **yt-dlp** | YouTube 视频下载引擎 | yt_download.py | 路径配置在 `yt_config.json` 的 `工具路径.yt-dlp`，也可在 PATH 中 |
| **ffmpeg** | 视频转码（webm → mp4）、音频提取 | yt_download.py | 路径配置在 `yt_config.json` 的 `工具路径.ffmpeg`，也可在 PATH 中 |

### 可选

| 工具 | 用途 | 被谁调用 | 备注 |
|------|------|---------|------|
| node.js | yt-dlp 签名参数处理 | yt_download.py | 如 yt-dlp 需要 js 运行时解算 n-challenge 签名时使用 |

---

## 系统基础设施

| 项目 | 用途 | 备注 |
|------|------|------|
| **代理服务器** | YouTube 访问代理 | 配置在 `yt_config.json` 的 `代理` 段；当前地址 `socks5://192.168.31.102:10888` |
| **WeChatAppEx 窗口** | 微信视频号发布目标窗口 | 人工登录授权后保持窗口打开，`run_workflow.py` 在此窗口上执行自动化操作 |

---

## 项目内部脚本依赖

```
run_workflow.py ──→ template_matcher.py           # 图像模板匹配
task_engine.py   ──→ feishu_send.py               # 飞书消息收发
publish_prepare.py ─→ yt_download_to_publish.py   # 工具函数（查找文件、解析元数据、渲染模板）
```

---

## 一次性设置清单

### 首次运行前需要完成的步骤

- [ ] 安装 Python 3.12+
- [ ] pip install 以上第三方包
- [ ] 安装 yt-dlp（`pip install yt-dlp` 或下载 exe）
- [ ] 安装 ffmpeg（pip install ffmpeg 或下载 exe）
- [ ] 配置 `yt_config.json` 中的工具路径和代理
- [ ] 飞书 CLI（lark-cli）登录认证
- [ ] 生成 YouTube cookies 文件（运行 `update_cookies.py`）
- [ ] 人工授权微信视频号并保持窗口打开
