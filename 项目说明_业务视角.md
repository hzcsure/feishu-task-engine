# YouTube 视频下载与微信视频号自动发布系统
## 项目说明文档（业务视角）

---

## 一、项目概述

本系统是一个基于**飞书机器人**驱动的自动化任务引擎，支持通过飞书消息远程登记任务、调度执行、查询状态，并完成以下两类核心业务：

1. **下载 YouTube 视频**：从 YouTube 下载指定视频，支持断点续传、转码、信息文件保存。
2. **下载 YouTube 视频并发布到微信视频号**：下载视频后，通过桌面自动化模拟人工操作，将视频发布到微信视频号。

用户无需登录服务器，只需在飞书聊天窗口发送消息，即可完成任务的登记、执行和监控。

---

## 二、业务场景

### 场景1：下载 YouTube 视频

**使用目的**：将 YouTube 上的视频保存到本地，用于存档、离线观看或二次剪辑。

**操作流程**：
1. 用户在飞书中发送：`登记任务`
2. 机器人回复任务类型列表，用户选择「下载YouTube视频」
3. 机器人发送表单，用户填写：
   - 视频链接（必填）：YouTube 视频 URL
   - 标题（可选）：自定义标题，留空则使用原视频标题
   - 描述（可选）：视频描述
   - 内容标签（可选）：逗号分隔的标签，如 `AI, 教程`
   - 执行时间（可选）：定时执行，留空立即执行
4. 任务登记成功后，引擎自动调度执行
5. 下载完成后，机器人通知用户

**输出产物**：
- 视频文件：保存在 `downloads/` 目录（如 `视频标题.mp4`）
- 信息文件：同名 `_info.txt`，包含标题、描述、时长、上传者等元数据

---

### 场景2：下载并发布到微信视频号

**使用目的**：将 YouTube 视频下载后，自动发布到微信视频号，无需人工操作。

**操作流程**：
1. 用户在飞书中发送：`登记任务`
2. 选择「下载YouTube并发布到微信视频号」
3. 填写表单：
   - 视频链接（必填）
   - 标题（必填）：视频号发布标题，6~16 字
   - 描述（可选）：视频描述
   - 内容标签（可选）：将自动转为 `#标签` 格式附加到描述前
   - 发布时间（可选）：定时发布，留空立即发布
   - 执行时间（可选）：任务定时执行
4. 任务登记后，引擎按两个阶段自动执行：
   - **阶段1**：下载视频（调用 `yt_download.py`）
   - **阶段2**：发布到视频号（调用 `run_workflow.py`，模拟鼠标键盘操作）
5. 每个阶段完成后，机器人通知进度
6. 全部完成后，机器人发送最终结果

**输出产物**：
- 视频文件：下载到 `downloads/`，复制到 `WeChatAppEx/` 供发布使用
- 发布记录：`.publish_history.json`，避免重复发布

---

## 三、飞书交互命令

用户通过飞书与机器人对话，支持以下命令：

| 命令 | 说明 | 示例 |
|------|------|------|
| `登记任务` | 开始登记新任务，机器人引导填写表单 | `登记任务` |
| `任务列表` | 查看所有任务及其状态 | `任务列表` |
| `查询任务 T-ID` | 查看指定任务的详细状态 | `查询任务 T-20260627-001` |
| `取消任务 T-ID` | 取消待执行或已失败的任务 | `取消任务 T-20260627-001` |
| `删除任务 T-ID` | 彻底删除任务（不含已下载文件） | `删除任务 T-20260627-001` |
| `删除全部任务` | 清空所有任务记录 | `删除全部任务` |
| `重试任务 T-ID` | 重新执行失败的任务（跳过已成功阶段） | `重试任务 T-20260627-001` |
| `帮助` | 查看可用命令列表 | `帮助` |

---

## 四、环境安装与依赖

### 4.1 一键安装

在项目目录下双击运行 `setup.bat`，自动完成以下安装：

| 步骤 | 内容 | 说明 |
|------|------|------|
| 1 | `pip install -r requirements.txt` | 安装 Python 第三方包（opencv、pyautogui、pywin32 等） |
| 2 | `pip install yt-dlp` | 安装 YouTube 下载引擎 |
| 3 | `pip install imageio-ffmpeg` | 安装视频转码引擎 |
| 4 | `python -m playwright install chromium` | 安装浏览器（用于更新 YouTube cookies） |

### 4.2 依赖清单

**Python 标准库**（内置，无需安装）：`os`、`sys`、`json`、`re`、`time`、`subprocess`、`threading`、`shutil`、`ctypes`、`pathlib`、`datetime`、`queue`、`copy`、`io`、`argparse`

**第三方包**（由 `requirements.txt` 管理）：

| 包名 | 用途 |
|------|------|
| `opencv-python` | 图像模板匹配 |
| `numpy` | 矩阵运算 |
| `pyautogui` | 桌面鼠标/键盘自动化 |
| `pyperclip` | 剪贴板操作（中文粘贴） |
| `Pillow` | 图像截取（ImageGrab） |
| `psutil` | 进程信息查询 |
| `pywin32` | Windows 窗口管理 |
| `playwright` | 浏览器自动化（更新 cookies） |

**外部系统工具**：

| 工具 | 用途 | 安装方式 |
|------|------|---------|
| `yt-dlp` | YouTube 视频下载 | `pip install yt-dlp` |
| `ffmpeg` | 视频转码（webm→mp4） | `pip install imageio-ffmpeg` |
| `lark-cli` | 飞书消息通信 | WorkBuddy 连接器管理 → 飞书 |

### 4.3 首次运行前手动配置

- [ ] 飞书 lark-cli 登录认证
- [ ] 运行 `update_cookies.py` 获取 YouTube cookies（`youtube_cookies.txt`）
- [ ] 确认 `yt_config.json` 中的代理地址和工具路径正确
- [ ] 人工登录微信视频号，保持 `WeChatAppEx` 窗口打开

---

## 五、系统启动与运行

### 启动引擎

在服务器上运行：

```bash
python task_engine.py
```

引擎启动后：
- 自动加载已有任务
- 发送启动通知到飞书聊天窗口
- 开始轮询飞书消息，等待用户指令

### 后台运行

如需后台长期运行：

```bash
python task_engine.py --daemon
```

---

## 六、任务状态说明

任务在执行过程中会经历以下状态：

| 状态 | 说明 | 可操作 |
|------|------|--------|
| `pending` | 待执行（等待调度或立即执行） | 可取消、可删除 |
| `running` | 正在执行中 | 不可取消（需等待完成） |
| `success` | 全部阶段执行成功 | 可删除 |
| `failed` | 部分阶段执行失败 | 可重试、可删除、可取消 |
| `cancelled` | 已取消 | 可删除 |

---

## 七、阶段执行说明（任务类型2）

「下载YouTube并发布到微信视频号」为**多阶段任务**，包含两个阶段：

### 阶段1：下载视频
- 调用 `yt_download.py` 从 YouTube 下载视频
- 输出：视频文件 + 信息文件（`_info.txt`）
- 失败后策略：**中止**（不执行阶段2）

### 阶段2：发布到视频号
- 调用 `run_workflow.py` 通过桌面自动化发布视频
- 前置准备（适配器自动完成）：
  1. 查找已下载的视频和信息文件
  2. 提取标题、描述
  3. 标题清理 + 长度处理（6~16字）
  4. 描述组装（标签前置 + 截断900字）
  5. 复制视频到发布目录（`WeChatAppEx/`）
  6. 渲染发布工作流模板
- 失败后策略：**通知继续**（记录失败但不中断）

---

## 八、去重与防重复发布

系统通过 `.publish_history.json` 记录已发布的视频 URL，避免同一视频重复发布到视频号。

如果用户误登记了已发布的视频，适配器会自动跳过发布阶段，并通知用户。

---

## 九、常见业务问题

### Q1：如何查看任务执行进度？
发送 `查询任务 T-ID`，机器人会返回当前各阶段的执行状态。

### Q2：下载失败了怎么办？
发送 `重试任务 T-ID`，系统会重新执行失败的阶段（已成功的阶段不会重复执行）。

### Q3：视频号发布失败了怎么办？
发布阶段失败后，系统会继续执行（策略为"通知继续"）。用户可：
- 检查 `WeChatAppEx/` 窗口是否正常
- 手动重试任务

### Q4：如何修改已登记的任务？
目前不支持修改，需删除后重新登记。

### Q5：下载的视频保存在哪里？
默认保存在 `downloads/` 目录，文件名为视频标题（经安全字符清理）。

---

## 十、注意事项

1. **YouTube Cookies**：下载 YouTube 视频需要有效的 cookies 文件（`youtube_cookies.txt`），过期后需运行 `python update_cookies.py` 更新。
2. **微信视频号窗口**：发布阶段需要 `WeChatAppEx` 进程窗口处于运行状态，且不能被最小化到托盘。
3. **模板图片**：`WeChatAppEx/` 目录下的 `.png` 图片是模板匹配参考图，不可删除。
4. **并发安全**：系统通过 stdin 传递配置，避免多任务同时发布时的配置冲突。

---

## 十一、项目文件清单

### 核心程序
| 文件 | 说明 |
|------|------|
| `task_engine.py` | 任务引擎主程序，负责消息接收、任务调度、状态管理 |
| `feishu_send.py` | 飞书通信模块，被 task_engine 调用 |
| `yt_download.py` | YouTube 视频下载器 |
| `run_workflow.py` | 桌面自动化工作流执行器 |
| `template_matcher.py` | 图像模板匹配引擎 |
| `update_cookies.py` | YouTube cookies 更新工具 |

### 适配器
| 文件 | 说明 |
|------|------|
| `adapters/__init__.py` | 适配器包初始化 |
| `adapters/publish_prepare.py` | 发布前准备适配器（标题清理、描述组装、模板渲染） |
| `adapters/yt_download_to_publish.py` | 下载→发布工具函数库 |

### 配置文件
| 文件 | 说明 |
|------|------|
| `task_configs.json` | 任务类型定义（表单字段、阶段配置） |
| `feishu_config.json` | 飞书聊天配置 |
| `yt_config.json` | YouTube 下载配置（代理、cookies、格式等） |
| `wf_publish.template.json` | 视频号发布工作流模板 |

### 数据文件
| 文件 | 说明 |
|------|------|
| `tasks.json` | 任务数据持久化存储 |
| `downloads/` | 下载的视频文件 |
| `WeChatAppEx/` | 发布目录 + 模板图片 |
| `.publish_history.json` | 已发布视频记录（自动生成） |
| `.browser_profile/` | 浏览器配置（用于更新 cookies） |
